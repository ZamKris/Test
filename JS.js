function selectnav(selection)
{
    let sel = document.getElementById(selection);

    switch(selection)
    {
        case 'Home':
                    break;
        case 'Portfolio':
                    break;
        case 'About': 
                    break;
        case 'Contact':   
                    break;
                
        default:;
    }
}